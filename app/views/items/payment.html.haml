%h1= @item.name + 'を購入します'
%p
  住所、氏名は空欄でも利用できます。
= form_tag buy_item_path(@item), html: { role: 'form' } do
  .form-group
    = label_tag 'address'
    %br
    = text_field_tag 'address', '', class: 'form-control'
  .form-group
    = label_tag 'name'
    %br
    = text_field_tag 'name', '', class: 'form-control'
  .form-group
    %script{src: 'https://checkout.webpay.jp/v2', class: 'webpay-button', data: { key: Rails.application.config.webpay_public_key, partial: 'true', lang: 'ja', text: 'カード情報を入力して購入'}}
